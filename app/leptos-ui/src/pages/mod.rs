pub mod beads;          // Kanban Board (tab 0)
pub mod agents;         // Agent Terminals (tab 1)
pub mod analytics;      // Insights (tab 2)
pub mod ideation;       // Ideation (tab 3)
pub mod roadmap;        // Roadmap (tab 4)
pub mod context;        // Context (tab 5)
pub mod mcp;            // MCP Overview (tab 6)
pub mod worktrees;      // Worktrees (tab 7)
pub mod github_issues;  // GitHub Issues (tab 8)
pub mod github_prs;     // GitHub PRs (tab 9)
pub mod claude_code;    // Claude Code (tab 10)
pub mod config;         // Settings (tab 11)
pub mod sessions;       // Keep for sub-navigation
pub mod convoys;        // Keep for sub-navigation
pub mod costs;          // Keep for sub-navigation
pub mod dashboard;      // Keep for compatibility
