pub mod beads;          // Kanban Board (tab 1)
pub mod agents;         // Agent Terminals (tab 2)
pub mod analytics;      // Keep for compatibility
pub mod insights;       // Insights chat (tab 3)
pub mod ideation;       // Ideation (tab 4)
pub mod roadmap;        // Roadmap (tab 5)
pub mod context;        // Context (tab 6)
pub mod mcp;            // MCP Overview (tab 7)
pub mod worktrees;      // Worktrees (tab 8)
pub mod github_issues;  // GitHub Issues (tab 9)
pub mod github_prs;     // GitHub PRs (tab 10)
pub mod claude_code;    // Claude Code (tab 11)
pub mod config;         // Settings (tab 12)
pub mod terminals;      // Terminals (tab 13)
pub mod sessions;       // Keep for sub-navigation
pub mod convoys;        // Keep for sub-navigation
pub mod costs;          // Keep for sub-navigation
pub mod dashboard;      // Keep for compatibility
